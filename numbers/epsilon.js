/**
 * 
 * Number.EPSILON returns the smallest interval between two representable numbers. This is useful for the problem with floating-point approximation.
 * 
 */

const getEPSILON = () => Number.EPSILON


// test
console.log(getEPSILON());